<mah:MetroWindow x:Class="ProjectsAndTasks.View.ProjectV"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectsAndTasks.ViewModel"
            xmlns:view="clr-namespace:ProjectsAndTasks.View"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="Tasks" Height="450" Width="800"
        TitleTemplate="{StaticResource ResourceKey=CustomWindowTitleTemplate}"
        TitleCharacterCasing="Normal"
        WindowTitleBrush="LightGray">
    <Grid>
        <view:TopMenu VerticalAlignment="Top"/>
        <ScrollViewer Margin="0,24,0,0">
            <StackPanel Background="LightGray">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <Button Content="Create new task" Command="{Binding CreateNewTaskCommand}" HorizontalAlignment="Left" Margin="5"/>
                    <Button Content="Return to Main Page" Command="{Binding ReturnToMainPageCommand}"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding NewTasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="5" Background="White">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}" Margin="0,0,5,0"/>
                                    <ComboBox SelectedValue="{Binding Progress}" SelectedValuePath="Tag">
                                        <ComboBoxItem Content="0%" Tag="0"/>
                                        <ComboBoxItem Content="10%" Tag="10"/>
                                        <ComboBoxItem Content="20%" Tag="20"/>
                                        <ComboBoxItem Content="30%" Tag="30"/>
                                        <ComboBoxItem Content="40%" Tag="40"/>
                                        <ComboBoxItem Content="50%" Tag="50"/>
                                        <ComboBoxItem Content="60%" Tag="60"/>
                                        <ComboBoxItem Content="70%" Tag="70"/>
                                        <ComboBoxItem Content="80%" Tag="80"/>
                                        <ComboBoxItem Content="90%" Tag="90"/>
                                        <ComboBoxItem Content="100%" Tag="100"/>
                                    </ComboBox>
                                    <Button Content="Save task" Command="{Binding SaveTaskChangesCommand}"/>
                                    <Button Content="Delete task" Command="{Binding RemoveTaskCommand}"/>
                                </StackPanel>
                                <TextBox Text="{Binding Description}" TextWrapping="Wrap" AcceptsReturn="True" VerticalAlignment="Top" MinHeight="30" MaxHeight="200" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Left"/>
                                <ProgressBar Value="{Binding Progress}" Minimum="0" Maximum="100" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</mah:MetroWindow>
